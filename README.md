# kstn46-pickerball

Dự án `kstn46-pickerball` — repository đã được khởi tạo sau khi clone từ GitHub.

Thêm mã nguồn, cấu hình build và tài liệu theo stack bạn chọn (web, mobile, v.v.).

## Clone

```bash
git clone https://github.com/thuypv281/kstn46-pickerball.git
cd kstn46-pickerball
```

## Đẩy code lên GitHub

```bash
git push -u origin main
```

(Nếu nhánh mặc định là `master`, đổi tên hoặc dùng `git push -u origin master`.)
